fastapi>=0.110
uvicorn[standard]>=0.27
requests>=2.31
apscheduler>=3.10
pytz>=2024.1                       # без него у бота НЕ работает расписание (JobQueue)
python-telegram-bot[job-queue]>=20.7
playwright>=1.40   # Booking.com price monitor (run once: playwright install chromium)
